export type President = {
    name: string,
    startDate: string,
    endDate: string,
    urlLogo: string
}

export type AccumulatedInflation = {
    id: null,
    total: number
}

export type AccumulatedWage = {
    initialValue: number,
    finalValue: number,
    percentual: number
}